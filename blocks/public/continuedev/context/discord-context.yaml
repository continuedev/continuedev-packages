name: Discord Context
version: 0.0.1
schema: v1

context:
  - provider: discord
    params:
      discordKey: ${{ inputs.DISCORD_KEY }}
      guildId: ${{ inputs.GUILD_ID }}
      channels: ${{ inputs.CHANNELS }}
