name: Codebase Context
version: 0.0.2
schema: v1

context:
  - provider: codebase
    params:
      nRetrieve: ${{ inputs.nRetrieve }}
      nFinal: ${{ inputs.nFinal }}
      useReranking: ${{ inputs.useReranking }}
