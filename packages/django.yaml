name: Django
version: 0.0.1

rules:
  - Follow Django style guide
  - Avoid using raw queries
  - Prefer the Django REST Framework for API development
  - Prefer Celery for background tasks
  - Prefer Redis for caching and task queues
  - Prefer PostgreSQL for production databases

prompts:
  - name: Create a basic Django model with common fields
    description: 
    prompt: Create a basic Django model with common fields
    - name: Add login required decorator
    description: 
    prompt: Add login required decorator
  - name: Create basic CRUD class-based views
    description: 
    prompt: Create basic CRUD class-based views

docs:
  - name: Django
    startUrl: https://docs.djangoproject.com/en/stable/
  
  - name: Django REST Framework
    startUrl: https://www.django-rest-framework.org/

  - name: Celery
    startUrl: https://docs.celeryq.dev/en/stable/

  - name: Redis
    startUrl: 

  - name: PostgreSQL
    startUrl: 